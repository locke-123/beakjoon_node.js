const fs = require('fs');
let input = parseInt(fs.readFileSync('example.txt').toString());
console.log(input);

function ap(n) { //등차수열 판별기
    //각 자릿수 분해 4673 참조
    //각 자릿수를 배열로 할당
    //배열의 각 두 수의 차이가 일정한지 확인
}